#include "main.h"

/**
 * print_alphabet_x10 - function to print 10 times the alphabet
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	char r, s;

	for (s = 0; s <= 9; s++)
	{
		for (r = 'a'; r <= 'z'; r++)
		{
			_putchar(c);
		}
		_putchar('\n')
	}
}
